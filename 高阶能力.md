# 真实 Claude CLI 桥接对接实施计划

将后端的 claudecode 桥接从 wexpect PTY 方案替换为 **subprocess + stream-json** 方案，对接本地真实 claudecode CLI，实现完整的 SDD 流程。

## 技术方案

claudecode CLI v2.1.63 支持 `--print --output-format stream-json --verbose` 模式，输出 **NDJSON**（每行一个 JSON 对象），事件类型如下：

| 事件 type | 说明 | 关键字段 |
|-----------|------|----------|
| [system](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#159-172) (subtype=init) | 会话初始化 | session_id, model, tools, cwd |
| [assistant](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#173-233) | AI 响应（含 thinking/text/tool_use） | message.content[].type |
| [result](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#133-140) (subtype=success/error) | 执行结束 | result, duration_ms, total_cost_usd |

[assistant](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#173-233) 消息的 `content` 数组包含：
- [thinking](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#106-111) — AI 思考过程（可折叠展示）
- `text` — AI 自然语言回复
- [tool_use](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#112-118) — 工具调用（Bash, Edit, Read 等）
- `tool_result` — 工具执行结果

---

## Proposed Changes

### 1. 后端 — 桥接引擎重写

#### [MODIFY] [claude_bridge.py](file:///g:/proj/SDD-native/backend/app/engine/claude_bridge.py)

**重大变更**：新增 [SubprocessCliBridge](file:///g:/proj/SDD-native/backend/app/engine/claude_bridge.py#49-234) 替代 `WexpectCliBridge`

```python
class SubprocessCliBridge(CliBridgeBase):
    """基于 asyncio subprocess + stream-json 的真实 CLI 桥接"""
    
    async def start_session(self, project_path, output_callback):
        # cmd /c claude -p --output-format stream-json --verbose
        # --dangerously-skip-permissions (可选)
        # 通过 stdin 管道发送 prompt
        self.process = await asyncio.create_subprocess_exec(
            "cmd", "/c", "claude", "-p",
            "--output-format", "stream-json", "--verbose",
            stdin=PIPE, stdout=PIPE, stderr=PIPE,
            cwd=project_path
        )
        # 启动异步读取循环，逐行读 stdout JSON
        self._reader_task = asyncio.create_task(self._read_loop())
        
    async def _read_loop(self):
        async for line in self.process.stdout:
            event = json.loads(line)
            await self._dispatch_event(event)
            
    async def _dispatch_event(self, event):
        # type=system → 提取 session_id
        # type=assistant → 解析 content 块 (thinking/text/tool_use)
        # type=result → 标记完成/失败
        self._output_cb(event)  # 结构化事件回调
```

- 保留 [MockCliBridge](file:///g:/proj/SDD-native/backend/app/engine/claude_bridge.py#236-321) 但不再使用 wexpect
- `get_cli_bridge()` 根据 `SDD_CLI_MODE` 选择 Mock 或 Subprocess

---

#### [MODIFY] [workflow_engine.py](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py)

- `handle_output()` 改为接收结构化事件 dict 而非原始字符串
- 新增事件分发逻辑：
  - [system](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#159-172) → 保存 session 信息
  - `assistant.thinking` → WebSocket 推送到前端 thinking 面板
  - `assistant.text` → WebSocket 推送为 chat_message (role=assistant)
  - `assistant.tool_use` → WebSocket 推送为 tool_use 日志  
  - [result](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#133-140) → 标记任务完成/失败，推送 status
- [run()](file:///g:/proj/SDD-native/test/test_claude_json.py#4-35) 方法简化：发送 prompt 到 stdin → 启动读取循环 → 等待 result 事件

---

#### [MODIFY] [websocket.py](file:///g:/proj/SDD-native/backend/app/schemas/websocket.py)

新增 payload schema：
- [WSThinkingPayload](file:///g:/proj/SDD-native/backend/app/schemas/websocket.py#60-63) — AI 思考过程推送
- [WSToolUsePayload](file:///g:/proj/SDD-native/backend/app/schemas/websocket.py#66-71) — 工具调用推送 (tool_name, input, output)

---

#### [MODIFY] [main.py](file:///g:/proj/SDD-native/backend/app/main.py)

WebSocket `chat_message` 处理增强：
- 接收用户 chat 消息 → 作为新 prompt 启动新的 CLI 进程或追加到 stdin

---

### 2. 前端 — 真实输出渲染

#### [MODIFY] [ChatView.vue](file:///g:/proj/SDD-native/frontend/src/views/ChatView.vue)

增强 [handleWsMessage()](file:///g:/proj/SDD-native/frontend/src/views/ChatView.vue#265-388) 和模板，处理新消息类型：

- **[thinking](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#106-111)** → 可折叠的 "思考" 面板（灰色背景，默认折叠）
- **`chat_message` (role=assistant)** → AI 回复气泡
- **[tool_use](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#112-118)** → 工具调用卡片（显示工具名和输入参数，可展开查看结果）
- **[status](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#119-124)** → 阶段进度卡片（session 初始化 / 执行完成）

---

## 配置切换

```env
# .env
SDD_CLI_MODE=real     # 使用真实 CLI
# SDD_CLI_MODE=mock   # 降级为 Mock
CLAUDE_CLI_PATH=claude
```

---

## Verification Plan

```bash
# 后端
cd g:\proj\SDD-native\backend
python -m uvicorn app.main:app --reload --port 8000

# 前端
cd g:\proj\SDD-native\frontend
npm run dev
```

1. 设置 `SDD_CLI_MODE=real` → 启动后端
2. 前端创建任务 → Start Engine → 输入 prompt
3. 验证：
   - 终端日志显示真实 CLI 输出
   - 聊天区出现 AI thinking（可折叠）+ text 回复
   - 工具调用（Bash/Edit 等）以卡片形式显示
   - 任务完成后显示 result 摘要（耗时、成本）
4. 发送追加消息 → 验证多轮对话

---

## 3. 增强功能：头脑风暴配置与高级 MCP 验证 (最新)

为了解决默认不调用 superpowers brainstorm 以及缺失代码生成后的高阶验证情况，计划增加如下实现：

### 3.1 创建任务可选 `/brainstorm`
- **Backend ([schemas/task.py](file:///g:/proj/SDD-native/backend/app/schemas/task.py), [routers/task.py](file:///g:/proj/SDD-native/backend/app/routers/task.py))**：在 [TaskCreate](file:///g:/proj/SDD-native/backend/app/schemas/task.py#10-14) 中增加 `use_brainstorm: Optional[bool] = False`。创建任务时如果为 true，将在 `description` 尾部追加强制指令 `"\n\n请强制调用 superpowers 中的 /brainstorm 能力进行头脑风暴。"`，利用原有的 prompt 传导机制传递给 CLI。
- **Frontend ([ChatView.vue](file:///g:/proj/SDD-native/frontend/src/views/ChatView.vue))**：在新建任务弹窗中加入 `使用 Brainstorm 进行初期架构头脑风暴` 的 Checkbox。

### 3.2 增加高阶 MCP 自动化验证快捷指令
- **Frontend ([ChatView.vue](file:///g:/proj/SDD-native/frontend/src/views/ChatView.vue))**：在聊天 Header 的 Action 栏（或聊天框输入侧旁）新增三个快速验证按钮，发送固定结构的指令进入同样的 WebSocket CLI 会话中：
  1. **UI 测试 (Playwright)**：向大模型发送指令调度 Playwright 进行纯前端页面点击跳转与 UI 渲染验证，并声明自动 Teardown。
  2. **API 测试 (Postman)**：发送指令要求大模型调度 Postman 验证 HTTP 断言与响应，并在结束后清理持久化脏数据。
  3. **E2E 全链路测试**：发送指令要求大模型双管齐下调度 Postman + Playwright，从 UI 触发链路穿透并在 DB/服务端做一致性检验，结束后自带清理。
- 采用向同一 WebSocket 下发 `chat_message` 的零成本方案复用目前的 `--resume` 热加载上下文架构，直接启动高阶验证任务。
