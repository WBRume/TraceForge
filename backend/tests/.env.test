# ─────────────────────────────────────────────────────────────────────────────
# 测试专用基础设施配置（tests/conftest.py 在任何 app 导入之前加载进环境变量）。
#
# 背景：app/config.py 硬编码读取 backend/.env（生产/开发共用），测试进程默认
# 会继承其中的远程基础设施地址——此前测试曾直连生产 Redis 并写入测试锁键。
# pydantic-settings 优先级：真实环境变量 > 本文件（以 override=False 注入
# os.environ）> .env 文件，因此本文件的值总是压过 .env；开发者显式 export
# 的变量又能压过本文件（用于按需开启 live 集成检查）。
#
# 原则：默认测试套件零远程依赖、完全离线可复现；真实基础设施检查必须
# 显式 opt-in（见 pytest.ini 的 live_revert 标记）。
# ─────────────────────────────────────────────────────────────────────────────

# 分布式锁：进程内实现。语义与 Redis 后端一致（互斥 / acquire-timeout → 409 / 释放），
# 业务测试不因远端延迟抖动而间歇性失败。
DISTRIBUTED_LOCK_BACKEND=local
DISTRIBUTED_LOCK_ALLOW_LOCAL_FALLBACK=true

# 兜底隔离：即便某个代码路径意外直连 Redis（get_redis_client），
# 也只会连本地空端口快速失败（连接超时 3s），绝不触及 .env 里的生产 Redis。
REDIS_URL=redis://127.0.0.1:6390/0
