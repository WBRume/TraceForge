# FastAPI Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Database
sqlalchemy==2.0.36
alembic==1.14.1
pymysql==1.1.1
cryptography==44.0.0

# Authentication
python-jose[cryptography]==3.3.0
bcrypt==5.0.0
# pydantic EmailStr 依赖
email-validator==2.3.0

# Configuration
pydantic-settings==2.7.1

# Logging
loguru==0.7.3

# PTY Bridge (Windows)
wexpect==4.0.0
# wexpect 依赖 pkg_resources（Python 3.12+ 的 venv 不再自带 setuptools；81.0 起 setuptools 移除了 pkg_resources）
setuptools==80.10.2

# WebSocket
websockets==14.1

# Utilities
python-dateutil==2.9.0
python-docx==1.1.2
PyYAML==6.0.3
redis>=5.0.0
zstandard==0.25.0
