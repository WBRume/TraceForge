项目是最上层，管理项目进度，每个项目的交付进度不同，项目应该包含产品，一个项目包含多个产品。
产品主要就是管理产品的版本，产品需要关联具体的多个仓库，勾选的时候可以直接勾选仓库组，每个产品需要填写具体git tag或者分支，用来后续新建工作区检出配置仓库的分支。
仓库中不需要包含版本信息，删除新建产品线和新建项目组，增加左侧树形列表可以分组管理仓库，增加仓库组功能，只是单纯的仓库管理页。

新建工作区选择项目后需要再勾选对应的产品。


补齐前端缺失的icon和样式,编辑、查看、返回等按钮

仓库管理当前存在问题如下：
1. 仓库组内的搜索应该仅能搜索仓库组
2. 仓库的搜索编码或者名称应该是基于当前选中的仓库组为根节点向下搜索，搜索的内容可以包含子节点，而不是仅能搜索当前仓库组内的仓库

产品管理当前存在问题如下：
产品在发布版本的时候，可能会基于当前所包含的仓库统一创建一个新的版本，如以下使用场景：
产品A的V1.0在研发过程中，此时会将产品A的v1.0绑定代码仓rep
产品A经过研发，v1.0进行发布，此时会将代码仓rep创建一个分支branch_v0.0.1分支，作为一个当前版本的持续维护版本
但是这个流程在当前的操作中却无法适用，这个地方需要重新设计；
我认为，产品创建的时候应该是没有版本的，然后操作人员对产品进行仓库录入，当然，这个东西是可以变动的
然后基于每个版本的研发流程或发布流程，可以引用前面创建的产品基础仓库，或统一绑定对应的仓库分支
当然还要具备灵活性，比如某个产品在某个特殊版本会有一个独立的仓库适用，其他的版本可能就没有这种仓库，所以这部分方案需要重新设计


产品对仓库进行绑定后，处于某个版本中的仓库，绑定的分支或者tag应该也是可以修改的，否则就没办法创建规划中的版本，也没办法将开发中的版本进行发布，因为一般都是发布的时候仓库会创建新分支


项目管理当前存在问题如下：
1. 项目生命周期回退按钮箭头位置很奇怪，需要改一下，改成箭头在按钮中的左边要和按钮中的问题水平对齐
2. 基于产品版本相关的修改，这里项目管理的时候绑定产品的版本选择形式会发生变化

依托项目、产品、仓库的路径，在删除的时候应该有校验，如删除仓库时需要确保仓库没有在产品和项目中引用，这种依赖路径校验需要有，并且在删除时给出提示


