# SDD Native 功能增强验收文档

本次更新重点解决了工作区属性缺失以及 Dashboard 交互链路不完整的问题。

## 主要变更

### 1. 工作区模型与配置扩展
- **后端模型**: 在 [Workspace](file:///g:/proj/SDD-native/backend/app/models/user.py#42-58) 表中新增了 [project_path](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#106-113) (项目生成物理路径) 和 `git_repo_url` (可选 Git 仓库)。
- **默认配置**: 用户注册时自动生成的默认工作区现在预设了 `./projects/{user_id}` 作为生成路径。
- **创建交互**: 在“管理工作区”页面，新建工作区弹窗增加了上述两项配置，其中“生成路径”为必填项。

### 2. Dashboard 任务创建集成
- **一键发起**: 在效能看板右上角新增了 `Start New Task` 按钮。
- **智能预填**: 点击按钮会弹出任务初始化 Modal，程序会自动从当前工作区的配置中读取并预填“生成路径”和“Git 地址”，减少重复输入。
- **自动跳转**: 任务创建成功后，系统会自动重定向至对应的 SDD Chat 会话页面。

## 验证结果

### 后端 API 验证
- [x] `POST /api/workspaces` 支持 [project_path](file:///g:/proj/SDD-native/backend/app/engine/workflow_engine.py#106-113) 和 `git_repo_url`。
- [x] `GET /api/workspaces/{id}` 返回完整配置。
- [x] `POST /api/workspaces/{ws_id}/tasks` 支持继承自工作区的默认路径。

### 前端 UI/UX 验证
- [x] Workspace 管理页面 Modal 布局正常，支持路径输入。
- [x] Dashboard 顶部按钮对齐，图标显示正常。
- [x] Dashboard 任务创建 Modal 交互流畅，支持快速跳转。

---
> [!TIP]
> 建议在重启服务器后，先前往“管理工作区”创建一个带有真实本地路径的新工作区，然后通过 Dashboard 发起任务，以获得最佳的自动化生成体验。
